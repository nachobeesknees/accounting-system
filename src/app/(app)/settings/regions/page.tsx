// The regions admin lives under /settings/regions. The original page was
// scaffolded at /regions; this re-exports it so both URLs resolve to the
// same UI without duplicating the form/CRUD code.
export { default } from "../../regions/page";
